# DASTAI-Alka

commands to run
---------------
Open Cmd. Navigate to Zap in C:/drive
Type this command -> zap.bat -daemon -port 8090 -config api.disablekey=true

Run below command in VS teminal
---------------------------------
uvicorn zap-ai-dast:app --reload
streamlit run ui.py
